FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY ingest.py .

# Kagglehub needs a home directory to cache downloads
ENV HOME=/tmp 

CMD ["python", "ingest.py"]