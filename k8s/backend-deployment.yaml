apiVersion: apps/v1
kind: Deployment
metadata:
  name: medical-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: medical-chatbot-backend
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8001
        env:
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: medical-chatbot-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: medical-chatbot-config
              key: POSTGRES_PORT
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: medical-chatbot-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: medical-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: medical-chatbot-config
              key: POSTGRES_DB
        - name: GRPC_HOST
          valueFrom:
            configMapKeyRef:
              name: medical-chatbot-config
              key: GRPC_HOST
        - name: GRPC_PORT
          valueFrom:
            configMapKeyRef:
              name: medical-chatbot-config
              key: GRPC_PORT
---
apiVersion: v1
kind: Service
metadata:
  name: medical-backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001